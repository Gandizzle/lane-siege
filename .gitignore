node_modules/
dist/
dist-ssr/
*.local
.DS_Store
.vite/
coverage/
*.log
.idea/
.vscode/*
!.vscode/extensions.json
android/

# Balance runs. The text report of a checkpoint run is committed; the raw
# records and the JSON summary are hundreds of KB of machine output and are
# reproducible from the seed in the report.
reports/*.json
